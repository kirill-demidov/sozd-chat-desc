# Настройка для GoDaddy

Если ваш домен `sozd-chat.org` хостится на GoDaddy, есть несколько вариантов настройки.

## 🎯 Рекомендуемый вариант: Поддомен `desc.sozd-chat.org`

Самый простой способ для GoDaddy - настроить поддомен вместо поддиректории.

### Шаг 1: Настройка DNS на GoDaddy

1. Войдите в панель управления GoDaddy: https://sso.godaddy.com/
2. Перейдите в **My Products** (Мои продукты)
3. Найдите домен `sozd-chat.org` и нажмите **DNS** (или "Управление DNS")
4. В разделе **Records** (Записи) найдите секцию **CNAME Records**
5. Нажмите **Add** (Добавить)
6. Заполните форму:
   - **Name** (Имя): `desc`
   - **Value** (Значение): `kirill-demidov.github.io`
   - **TTL** (время жизни): `600` секунд (или оставьте по умолчанию)
7. Нажмите **Save** (Сохранить)

### Шаг 2: Обновление CNAME в репозитории

После настройки DNS обновите файл `CNAME` в репозитории на `desc.sozd-chat.org`:

```bash
git add CNAME
git commit -m "Update CNAME for subdomain"
git push origin main
```

Или вручную измените содержимое файла `CNAME` на:
```
desc.sozd-chat.org
```

### Шаг 3: Ожидание распространения DNS

Подождите 10-30 минут для распространения DNS изменений.

### Шаг 4: Проверка

Откройте в браузере: `https://desc.sozd-chat.org`

---

## Альтернативный вариант: Основной домен на GitHub Pages

Если весь домен `sozd-chat.org` может быть на GitHub Pages:

### Шаг 1: Настройка DNS на GoDaddy

1. Войдите в панель управления GoDaddy
2. Перейдите в **DNS** для домена `sozd-chat.org`
3. В разделе **A Records** (A-записи) добавьте 4 записи:
   - **Name**: `@` (или оставьте пустым для корневого домена)
   - **Value**: `185.199.108.153`
   - **TTL**: `600`
   
   Повторите для остальных IP:
   - `185.199.109.153`
   - `185.199.110.153`
   - `185.199.111.153`

4. (Опционально) Для www добавьте **CNAME Record**:
   - **Name**: `www`
   - **Value**: `kirill-demidov.github.io`
   - **TTL**: `600`

### Шаг 2: Настройка в репозитории

Убедитесь, что файл `CNAME` содержит:
```
sozd-chat.org
```

### Шаг 3: Размещение контента

В этом случае GitHub Pages будет раздавать контент из корня домена. Если вам нужна поддиректория `/desc`, вам нужно будет настроить основной репозиторий сайта так, чтобы он включал содержимое этого репозитория в поддиректорию `/desc`.

---

## ❓ Часто задаваемые вопросы

**Q: Почему не работает поддиректория `/desc` на GoDaddy?**  
A: GoDaddy не предоставляет возможности настройки reverse proxy для обычных пользователей. Для поддиректорий нужен доступ к nginx или использование Cloudflare.

**Q: Можно ли использовать Cloudflare с GoDaddy?**  
A: Да! Вы можете изменить DNS серверы на Cloudflare и использовать их возможности для настройки поддиректорий.

**Q: Как долго распространяются DNS изменения?**  
A: Обычно 10-30 минут, но может занять до 48 часов (редко).

**Q: Как проверить, что DNS изменения применились?**  
A: Используйте команду в терминале:
```bash
dig desc.sozd-chat.org
# или
nslookup desc.sozd-chat.org
```

---

## 📝 Полезные ссылки

- [GoDaddy DNS Management](https://www.godaddy.com/help/manage-dns-records-680)
- [GitHub Pages Custom Domain](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)
- [GitHub Pages IP addresses](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site/managing-a-custom-domain-for-your-github-pages-site#configuring-an-apex-domain)
